# Sanity (seccion Clientes, ver src/lib/sanity.ts).
# En Cloudflare estas dos ya estan cargadas como variables de entorno del
# Worker. Para desarrollo local, copia este archivo a ".env.local" (esta en
# .gitignore, no se sube) y pon tus valores reales.
SANITY_PROJECT_ID=
SANITY_DATASET=
